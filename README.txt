Steps to set up database and run code

1. Create a database using CREATE DATABASE <name> and then use \c <name> to connect.

2. Run the db.sql script to create the tables.

3. Edit the fields on line 7 of app.py for username, password, and database name.

4. Use the command python app.py to run the app.

5. Make calls to the API
